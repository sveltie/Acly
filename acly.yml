name: acly
scope: source.acly

fileTypes: [acly]

patterns:
  - include: '#strings'
  - include: '#keywords'
  - include: '#identifiers'

strings:
  - name: string
    begin: '"'
    end: '"'
  - name: string
    begin: "'"
    end: "'"

keywords:
  - name: keyword.declaration
    match: 'fn'

identifiers:
  - name: identifiers
    match: '[a-zA-Z_][a-zA-Z0-9_]*'